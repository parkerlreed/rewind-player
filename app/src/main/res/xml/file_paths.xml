<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloads can live anywhere on shared storage, including SD cards (root-path). -->
    <external-path name="storage" path="." />
    <root-path name="root" path="." />
</paths>
